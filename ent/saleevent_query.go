// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"gitlab.calendaria.team/services/platform-billing/ent/predicate"
	"gitlab.calendaria.team/services/platform-billing/ent/saleevent"
)

// SaleEventQuery is the builder for querying SaleEvent entities.
type SaleEventQuery struct {
	config
	ctx        *QueryContext
	order      []saleevent.OrderOption
	inters     []Interceptor
	predicates []predicate.SaleEvent
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SaleEventQuery builder.
func (seq *SaleEventQuery) Where(ps ...predicate.SaleEvent) *SaleEventQuery {
	seq.predicates = append(seq.predicates, ps...)
	return seq
}

// Limit the number of records to be returned by this query.
func (seq *SaleEventQuery) Limit(limit int) *SaleEventQuery {
	seq.ctx.Limit = &limit
	return seq
}

// Offset to start from.
func (seq *SaleEventQuery) Offset(offset int) *SaleEventQuery {
	seq.ctx.Offset = &offset
	return seq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (seq *SaleEventQuery) Unique(unique bool) *SaleEventQuery {
	seq.ctx.Unique = &unique
	return seq
}

// Order specifies how the records should be ordered.
func (seq *SaleEventQuery) Order(o ...saleevent.OrderOption) *SaleEventQuery {
	seq.order = append(seq.order, o...)
	return seq
}

// First returns the first SaleEvent entity from the query.
// Returns a *NotFoundError when no SaleEvent was found.
func (seq *SaleEventQuery) First(ctx context.Context) (*SaleEvent, error) {
	nodes, err := seq.Limit(1).All(setContextOp(ctx, seq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{saleevent.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (seq *SaleEventQuery) FirstX(ctx context.Context) *SaleEvent {
	node, err := seq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first SaleEvent ID from the query.
// Returns a *NotFoundError when no SaleEvent ID was found.
func (seq *SaleEventQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = seq.Limit(1).IDs(setContextOp(ctx, seq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{saleevent.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (seq *SaleEventQuery) FirstIDX(ctx context.Context) int64 {
	id, err := seq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single SaleEvent entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one SaleEvent entity is found.
// Returns a *NotFoundError when no SaleEvent entities are found.
func (seq *SaleEventQuery) Only(ctx context.Context) (*SaleEvent, error) {
	nodes, err := seq.Limit(2).All(setContextOp(ctx, seq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{saleevent.Label}
	default:
		return nil, &NotSingularError{saleevent.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (seq *SaleEventQuery) OnlyX(ctx context.Context) *SaleEvent {
	node, err := seq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only SaleEvent ID in the query.
// Returns a *NotSingularError when more than one SaleEvent ID is found.
// Returns a *NotFoundError when no entities are found.
func (seq *SaleEventQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = seq.Limit(2).IDs(setContextOp(ctx, seq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{saleevent.Label}
	default:
		err = &NotSingularError{saleevent.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (seq *SaleEventQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := seq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of SaleEvents.
func (seq *SaleEventQuery) All(ctx context.Context) ([]*SaleEvent, error) {
	ctx = setContextOp(ctx, seq.ctx, ent.OpQueryAll)
	if err := seq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*SaleEvent, *SaleEventQuery]()
	return withInterceptors[[]*SaleEvent](ctx, seq, qr, seq.inters)
}

// AllX is like All, but panics if an error occurs.
func (seq *SaleEventQuery) AllX(ctx context.Context) []*SaleEvent {
	nodes, err := seq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of SaleEvent IDs.
func (seq *SaleEventQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if seq.ctx.Unique == nil && seq.path != nil {
		seq.Unique(true)
	}
	ctx = setContextOp(ctx, seq.ctx, ent.OpQueryIDs)
	if err = seq.Select(saleevent.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (seq *SaleEventQuery) IDsX(ctx context.Context) []int64 {
	ids, err := seq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (seq *SaleEventQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, seq.ctx, ent.OpQueryCount)
	if err := seq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, seq, querierCount[*SaleEventQuery](), seq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (seq *SaleEventQuery) CountX(ctx context.Context) int {
	count, err := seq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (seq *SaleEventQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, seq.ctx, ent.OpQueryExist)
	switch _, err := seq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (seq *SaleEventQuery) ExistX(ctx context.Context) bool {
	exist, err := seq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SaleEventQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (seq *SaleEventQuery) Clone() *SaleEventQuery {
	if seq == nil {
		return nil
	}
	return &SaleEventQuery{
		config:     seq.config,
		ctx:        seq.ctx.Clone(),
		order:      append([]saleevent.OrderOption{}, seq.order...),
		inters:     append([]Interceptor{}, seq.inters...),
		predicates: append([]predicate.SaleEvent{}, seq.predicates...),
		// clone intermediate query.
		sql:  seq.sql.Clone(),
		path: seq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.SaleEvent.Query().
//		GroupBy(saleevent.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (seq *SaleEventQuery) GroupBy(field string, fields ...string) *SaleEventGroupBy {
	seq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SaleEventGroupBy{build: seq}
	grbuild.flds = &seq.ctx.Fields
	grbuild.label = saleevent.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.SaleEvent.Query().
//		Select(saleevent.FieldCreatedAt).
//		Scan(ctx, &v)
func (seq *SaleEventQuery) Select(fields ...string) *SaleEventSelect {
	seq.ctx.Fields = append(seq.ctx.Fields, fields...)
	sbuild := &SaleEventSelect{SaleEventQuery: seq}
	sbuild.label = saleevent.Label
	sbuild.flds, sbuild.scan = &seq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SaleEventSelect configured with the given aggregations.
func (seq *SaleEventQuery) Aggregate(fns ...AggregateFunc) *SaleEventSelect {
	return seq.Select().Aggregate(fns...)
}

func (seq *SaleEventQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range seq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, seq); err != nil {
				return err
			}
		}
	}
	for _, f := range seq.ctx.Fields {
		if !saleevent.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if seq.path != nil {
		prev, err := seq.path(ctx)
		if err != nil {
			return err
		}
		seq.sql = prev
	}
	return nil
}

func (seq *SaleEventQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*SaleEvent, error) {
	var (
		nodes = []*SaleEvent{}
		_spec = seq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*SaleEvent).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &SaleEvent{config: seq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(seq.modifiers) > 0 {
		_spec.Modifiers = seq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, seq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (seq *SaleEventQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := seq.querySpec()
	if len(seq.modifiers) > 0 {
		_spec.Modifiers = seq.modifiers
	}
	_spec.Node.Columns = seq.ctx.Fields
	if len(seq.ctx.Fields) > 0 {
		_spec.Unique = seq.ctx.Unique != nil && *seq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, seq.driver, _spec)
}

func (seq *SaleEventQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(saleevent.Table, saleevent.Columns, sqlgraph.NewFieldSpec(saleevent.FieldID, field.TypeInt64))
	_spec.From = seq.sql
	if unique := seq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if seq.path != nil {
		_spec.Unique = true
	}
	if fields := seq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, saleevent.FieldID)
		for i := range fields {
			if fields[i] != saleevent.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := seq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := seq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := seq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := seq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (seq *SaleEventQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(seq.driver.Dialect())
	t1 := builder.Table(saleevent.Table)
	columns := seq.ctx.Fields
	if len(columns) == 0 {
		columns = saleevent.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if seq.sql != nil {
		selector = seq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if seq.ctx.Unique != nil && *seq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range seq.modifiers {
		m(selector)
	}
	for _, p := range seq.predicates {
		p(selector)
	}
	for _, p := range seq.order {
		p(selector)
	}
	if offset := seq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := seq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (seq *SaleEventQuery) Modify(modifiers ...func(s *sql.Selector)) *SaleEventSelect {
	seq.modifiers = append(seq.modifiers, modifiers...)
	return seq.Select()
}

// SaleEventGroupBy is the group-by builder for SaleEvent entities.
type SaleEventGroupBy struct {
	selector
	build *SaleEventQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (segb *SaleEventGroupBy) Aggregate(fns ...AggregateFunc) *SaleEventGroupBy {
	segb.fns = append(segb.fns, fns...)
	return segb
}

// Scan applies the selector query and scans the result into the given value.
func (segb *SaleEventGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, segb.build.ctx, ent.OpQueryGroupBy)
	if err := segb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SaleEventQuery, *SaleEventGroupBy](ctx, segb.build, segb, segb.build.inters, v)
}

func (segb *SaleEventGroupBy) sqlScan(ctx context.Context, root *SaleEventQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(segb.fns))
	for _, fn := range segb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*segb.flds)+len(segb.fns))
		for _, f := range *segb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*segb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := segb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SaleEventSelect is the builder for selecting fields of SaleEvent entities.
type SaleEventSelect struct {
	*SaleEventQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ses *SaleEventSelect) Aggregate(fns ...AggregateFunc) *SaleEventSelect {
	ses.fns = append(ses.fns, fns...)
	return ses
}

// Scan applies the selector query and scans the result into the given value.
func (ses *SaleEventSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ses.ctx, ent.OpQuerySelect)
	if err := ses.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SaleEventQuery, *SaleEventSelect](ctx, ses.SaleEventQuery, ses, ses.inters, v)
}

func (ses *SaleEventSelect) sqlScan(ctx context.Context, root *SaleEventQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ses.fns))
	for _, fn := range ses.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ses.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ses.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (ses *SaleEventSelect) Modify(modifiers ...func(s *sql.Selector)) *SaleEventSelect {
	ses.modifiers = append(ses.modifiers, modifiers...)
	return ses
}
