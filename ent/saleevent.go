// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/shopspring/decimal"
	"github.com/makesalekz/platform-billing/ent/saleevent"
)

// SaleEvent is the model entity for the SaleEvent schema.
type SaleEvent struct {
	config `json:"-"`
	// ID of the ent.
	ID int64 `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// TenantID holds the value of the "tenant_id" field.
	TenantID int64 `json:"tenant_id,omitempty"`
	// SaleID holds the value of the "sale_id" field.
	SaleID int64 `json:"sale_id,omitempty"`
	// ProductID holds the value of the "product_id" field.
	ProductID int64 `json:"product_id,omitempty"`
	// Amount holds the value of the "amount" field.
	Amount decimal.Decimal `json:"amount,omitempty"`
	// CompletedAt holds the value of the "completed_at" field.
	CompletedAt time.Time `json:"completed_at,omitempty"`
	// IsReturn holds the value of the "is_return" field.
	IsReturn     bool `json:"is_return,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SaleEvent) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case saleevent.FieldAmount:
			values[i] = new(decimal.Decimal)
		case saleevent.FieldIsReturn:
			values[i] = new(sql.NullBool)
		case saleevent.FieldID, saleevent.FieldTenantID, saleevent.FieldSaleID, saleevent.FieldProductID:
			values[i] = new(sql.NullInt64)
		case saleevent.FieldCreatedAt, saleevent.FieldUpdatedAt, saleevent.FieldCompletedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SaleEvent fields.
func (se *SaleEvent) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case saleevent.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			se.ID = int64(value.Int64)
		case saleevent.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				se.CreatedAt = value.Time
			}
		case saleevent.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				se.UpdatedAt = value.Time
			}
		case saleevent.FieldTenantID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field tenant_id", values[i])
			} else if value.Valid {
				se.TenantID = value.Int64
			}
		case saleevent.FieldSaleID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field sale_id", values[i])
			} else if value.Valid {
				se.SaleID = value.Int64
			}
		case saleevent.FieldProductID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field product_id", values[i])
			} else if value.Valid {
				se.ProductID = value.Int64
			}
		case saleevent.FieldAmount:
			if value, ok := values[i].(*decimal.Decimal); !ok {
				return fmt.Errorf("unexpected type %T for field amount", values[i])
			} else if value != nil {
				se.Amount = *value
			}
		case saleevent.FieldCompletedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field completed_at", values[i])
			} else if value.Valid {
				se.CompletedAt = value.Time
			}
		case saleevent.FieldIsReturn:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field is_return", values[i])
			} else if value.Valid {
				se.IsReturn = value.Bool
			}
		default:
			se.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SaleEvent.
// This includes values selected through modifiers, order, etc.
func (se *SaleEvent) Value(name string) (ent.Value, error) {
	return se.selectValues.Get(name)
}

// Update returns a builder for updating this SaleEvent.
// Note that you need to call SaleEvent.Unwrap() before calling this method if this SaleEvent
// was returned from a transaction, and the transaction was committed or rolled back.
func (se *SaleEvent) Update() *SaleEventUpdateOne {
	return NewSaleEventClient(se.config).UpdateOne(se)
}

// Unwrap unwraps the SaleEvent entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (se *SaleEvent) Unwrap() *SaleEvent {
	_tx, ok := se.config.driver.(*txDriver)
	if !ok {
		panic("ent: SaleEvent is not a transactional entity")
	}
	se.config.driver = _tx.drv
	return se
}

// String implements the fmt.Stringer.
func (se *SaleEvent) String() string {
	var builder strings.Builder
	builder.WriteString("SaleEvent(")
	builder.WriteString(fmt.Sprintf("id=%v, ", se.ID))
	builder.WriteString("created_at=")
	builder.WriteString(se.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(se.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("tenant_id=")
	builder.WriteString(fmt.Sprintf("%v", se.TenantID))
	builder.WriteString(", ")
	builder.WriteString("sale_id=")
	builder.WriteString(fmt.Sprintf("%v", se.SaleID))
	builder.WriteString(", ")
	builder.WriteString("product_id=")
	builder.WriteString(fmt.Sprintf("%v", se.ProductID))
	builder.WriteString(", ")
	builder.WriteString("amount=")
	builder.WriteString(fmt.Sprintf("%v", se.Amount))
	builder.WriteString(", ")
	builder.WriteString("completed_at=")
	builder.WriteString(se.CompletedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("is_return=")
	builder.WriteString(fmt.Sprintf("%v", se.IsReturn))
	builder.WriteByte(')')
	return builder.String()
}

// SaleEvents is a parsable slice of SaleEvent.
type SaleEvents []*SaleEvent
